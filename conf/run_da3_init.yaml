# @package _global_
defaults:
  - paths: base
  - hydra: base
  - compute: development
  - _self_

task_name: run_da3_init
run_mode: ???
experiment: ${hydra:job.override_dirname}
tags:
  - ${run_mode}
  - ${task_name}

data: ${paths.root_dir}/data/garden
images_subdir: images_4
colmap_subdir: sparse/0
out: ${paths.output_dir}
n_views: 30
model_id: depth-anything/DA3-GIANT-1.1
process_res: 504
